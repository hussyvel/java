# ============================================================
#  Configurações da aplicação — ajuste conforme seu ambiente
# ============================================================

# Servidor
server.port=8080

# Banco de dados PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/patrimonio
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Encoding
spring.mandatory-file-encoding=UTF-8
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.force=true

# ============================================================
#  Configurações de E-mail — ajuste conforme seu provedor
# ============================================================

 --- Gmail ---
 spring.mail.host=smtp.gmail.com
 spring.mail.port=587
 spring.mail.username=hussyribeiro@gmail.com
 spring.mail.password=esaaceytpjtvrbic
 spring.mail.properties.mail.smtp.auth=true
 spring.mail.properties.mail.smtp.starttls.enable=true

# --- Outlook / Hotmail ---
# spring.mail.host=smtp.office365.com
# spring.mail.port=587
# spring.mail.username=seuemail@outlook.com
# spring.mail.password=sua_senha
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true

# --- SMTP próprio (ex: servidor do governo) ---
# spring.mail.host=mail.seudominio.gov.br
# spring.mail.port=587
# spring.mail.username=sistema@seudominio.gov.br
# spring.mail.password=sua_senha
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true

# Descomente e preencha O BLOCO do seu provedor acima
# Remetente que aparece no e-mail:
spring.mail.properties.mail.smtp.from=hussyribeiro@gmail.com
